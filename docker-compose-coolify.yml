services:
    surrealdb:
      image: 'surrealdb/surrealdb:v2'
      command: 'start --log info --user root --pass root rocksdb:/mydata/mydatabase.db'
      user: root
      volumes:
        - './surreal_data:/mydata'
      environment:
        - SURREAL_EXPERIMENTAL_GRAPHQL=true
      restart: always
      pull_policy: always
    open_notebook:
      image: 'lfnovo/open_notebook:v1-latest'
      environment:
        - SERVICE_FQDN_OPENNOTEBOOK_8502=https://open-notebook.hr-applab.de
        - OPEN_NOTEBOOK_ENCRYPTION_KEY=HIER-ECHTES-SECRET-EINSETZEN
        - 'SURREAL_URL=ws://surrealdb:8000/rpc'
        - SURREAL_USER=root
        - SURREAL_PASSWORD=root
        - SURREAL_NAMESPACE=open_notebook
        - SURREAL_DATABASE=open_notebook
      volumes:
        - './notebook_data:/app/data'
      depends_on:
        - surrealdb
      restart: always
      pull_policy: always
